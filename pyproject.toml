[project]
name = "dect-convergence-validation"
version = "0.1.0"
description = "Reproducible numerical experiments for the dynamic dual-energy CT convergence study"
requires-python = ">=3.13,<3.14"
dependencies = [
    "matplotlib==3.11.1",
    "numpy==2.5.3",
    "scipy==1.18.1",
]

[tool.uv]
package = false
